# TKAI Framework V1.2 RC-3 Release Report

## Release baseline

- Baseline commit: `5de7b0d chore(release): finalize rc2 validation and release report`
- Candidate version: `1.2.0rc3`
- Scope: packaging, installation, metadata, import, CLI, Doctor, and
  compatibility validation only. No runtime feature or public API is added.
- Full regression suite: `326 passed`.
- Benchmark, stress, and reliability suites: 8 benchmark modules, 15 stress
  tests, and 11 reliability tests; all execute offline.

## Validation status

| Check | Status | Notes |
|---|---|---|
| Version consistency | Pass | `pyproject.toml`, `tkai.__version__`, README, CLI, and current release documents use `1.2.0rc3` |
| Package metadata | Pass | Name, Python range, MIT license, dependencies, build backend, and CLI entry point are declared |
| License | Pass | Standard MIT text is present in `LICENSE`; package metadata and README declare MIT |
| Wheel | Pass | `pip wheel . --no-deps --no-build-isolation` produced `tkai-1.2.0rc3-py3-none-any.whl` |
| Sdist | Not validated | The local `build` module is unavailable; no network installation was attempted |
| Distribution content | Wheel pass; sdist not validated | Wheel contains required template resources and excludes caches, secrets, and local files |
| Fresh install | Pass | Temporary local environment installed the wheel with `--no-deps`, then imported TKAI and ran CLI/Doctor smoke |
| Import smoke | Pass | Core, configuration, command, template, policy, retry, observability, telemetry, distributed, adaptive, multi-region, and runtime surfaces |
| CLI smoke | Pass | Help, version, environment Doctor, and JSON AI Doctor execute without provider configuration |
| API compatibility | Pass | Existing compatibility and public-import regressions remain in the suite |
| Configuration compatibility | Pass | Existing configuration regressions remain in the suite |

## Distribution rules

Release artifacts must contain the `tkai` package and metadata-required
documentation, but must not contain repositories, caches, credentials, local
environment files, benchmark output, or local absolute paths. Generated wheel,
sdist, and temporary virtual environments are validation artifacts only and are
not committed.

The initial RC-3 wheel audit found that `templates/default/template.json` and
the default template README were missing. The release-blocking packaging defect
was fixed with a minimal `tool.setuptools.package-data` declaration and the
rebuilt wheel includes both files.

## Known limitations

- The standard `build` module may be unavailable in an offline environment; an
  offline `pip wheel --no-deps` fallback is used where possible.
- RC-3 does not validate real provider traffic, distributed backends, active
  health probes, automatic failover, Redis, or OpenTelemetry.
- RC-3 does not create a GA tag or publish an artifact.

## GA recommendation

**Ready**, subject to the remaining manual release controls below. The RC-3 GA
license blocker is resolved with the MIT License, matching package metadata,
and README documentation. No GA tag is created by this validation work.

## Manual steps before GA

1. Run artifact build and inspect wheel and sdist contents in the target release
   environment.
2. Install the local wheel in a fresh environment, then repeat CLI and Doctor
   smoke checks.
3. Obtain release-owner approval, push the branch, and create a tag only after
   all release gates are green.
