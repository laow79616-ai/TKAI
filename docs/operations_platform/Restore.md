# Restore

Restore validates backup scope, status, and checksum. Preview mode is read-only.
A real restore requires an approval reference and produces validation, rollback,
and verification results. Storage adapters remain responsible for atomic
restoration and durable recovery points.
