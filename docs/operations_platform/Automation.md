# Automation

Automation supports scheduled, maintenance, cleanup, optimization, rotation,
and repair jobs. Payloads contain configuration references, never secrets.
Adapters execute jobs idempotently and report lifecycle transitions.
