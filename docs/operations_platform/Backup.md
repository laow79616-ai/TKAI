# Backup

Backups may include configuration, metadata, knowledge, workflow, application,
and policy state. Each backup records schedule, retention, validation checksum,
scope, status, and creation time. Production adapters should encrypt backup
artifacts and enforce retention independently of the control-plane record.
