# Operations

An operations center contains ID, name, description, owner, tenant, workspace,
status, and metadata. The dashboard combines operations, health, backups,
capacity, automation, diagnostics, events, notifications, and reports. Jobs
have explicit pending, running, succeeded, failed, and cancelled states.
