# Capacity

Capacity snapshots track CPU, memory, storage, token usage, queue depth,
concurrency, and forecast data. Invalid percentages are rejected. Threshold
breaches produce capacity alerts and operations events for automation.
