# Operations

Operators monitor BI resource totals, query failures and latency, and export
volume. Query and export limits are configurable, and all privileged operations
produce scoped audit entries without secret values.
