# Security

The control plane requires tenant/workspace isolation and RBAC. Row and column
security and sensitive-field masking are adapter interfaces. Credentials remain
opaque references, logs exclude secrets, and queries and exports are bounded.
