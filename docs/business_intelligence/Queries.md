# Queries

Queries select a dataset, governed metrics and dimensions, filters, sort order,
pagination, time range, row limit, and timeout. Adapters compile this contract;
unrestricted arbitrary SQL is not accepted by default.
