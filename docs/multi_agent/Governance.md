# Governance

Every stateful operation enforces RBAC plus tenant and workspace isolation.
Admin policies cover approvals and safety boundaries and accept references
rather than raw secrets. Agent actions, lifecycle changes, coordination,
delegation, consensus, execution, and policy changes are auditable.
