# Memory Namespaces

Namespaces partition memories inside a tenant and workspace. They are
registered when memory is created, listed only within the caller's scope, and
may be supplied as retrieval filters. Namespace names are application-defined;
recommended forms are stable paths such as `agents/support` or
`workflows/onboarding`.
