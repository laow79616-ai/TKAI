# Optimization

Optimization normalizes resource allocation and preserves dependency-safe execution
order while estimating cost and latency. Negative resource, cost, and latency
inputs are clamped so optimization cannot produce invalid operational estimates.
