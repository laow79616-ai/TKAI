# Validation

Validation covers constraints, structural consistency, dependency loops,
self-conflicts, and safety policy decisions. Results contain explicit failures and
warnings. Failures increment `reasoning_validation_failures_total` and are retained
as session artifacts for audit and explanation.
