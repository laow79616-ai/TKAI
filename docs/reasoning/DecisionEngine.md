# Decision Engine

The decision engine applies caller-declared rules, normalizes criterion scores,
ranks candidates deterministically, enforces a threshold, calculates confidence,
and selects a configured fallback when no candidate meets the threshold.
