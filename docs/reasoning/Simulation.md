# Simulation

Simulation accepts bounded scenarios, variables, baselines, and rollback steps.
Each result includes a deterministic prediction, evaluation score, baseline
comparison, and rollback plan. Simulation counts are enforced before execution.
