# Topology

Topology nodes represent services, agents, models, pipelines, and
infrastructure. Dependencies create directed edges and must reference existing
nodes in the same tenant and workspace. The API publishes nodes, edges, and a
health map suitable for dashboard visualization.
