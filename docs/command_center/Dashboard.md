# Dashboard

The Enterprise AI Command Center dashboard provides Overview, Operations,
Agents, Automation, Incidents, Alerts, Topology, Health, Activity, and Audit
views. The dashboard uses only the `/command-center/*` API and passes explicit
tenant, workspace, and actor context. It never renders credentials.
