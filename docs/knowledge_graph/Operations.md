# Operations

Monitor the eight `knowledge_*` Prometheus metrics, particularly query count and
cumulative latency. Bound query timeout, depth, and result limits. Index
refresh/optimization and vector stores remain replaceable adapters. Back up
graphs together with schemas, semantics, lineage, provenance, and audit data.
