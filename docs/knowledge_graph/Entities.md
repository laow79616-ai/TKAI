# Entities

Supported entity types are Person, Organization, Application, Agent, Model,
Dataset, Workflow, Document, Asset, and Custom. IDs are unique within the
platform and each entity belongs to exactly one graph, tenant, and workspace.
