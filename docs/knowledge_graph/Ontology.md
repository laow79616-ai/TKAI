# Ontology

Ontologies contain versioned classes, subclasses, properties, constraints,
vocabulary, and declarative inference rules. Only the documented rule keys are
accepted; rules are data and are never evaluated as executable code.
