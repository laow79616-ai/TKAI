# Queries

The graph query interface supports entity and relationship lookup, typed
matching, property patterns, aggregation projections, explicit timeouts, and
result limits. It does not interpret database query strings or executable code.
