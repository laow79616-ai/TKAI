# Schema

Schemas declare allowed entity and relationship types, required and optional
properties, validation metadata, and a version. Entity writes validate required
fields before storage. Primitive values, collections, references, temporal and
spatial values are represented as safe structured data.
