# Analytics

Analytics projections include degree centrality, connectivity, connected
communities, path counts, influence ordering, type coverage, and graph growth.
All computations operate only on the caller's tenant and workspace.
