# Security Handbook

Report vulnerabilities privately. Never commit secrets; use an approved secret manager. Apply least privilege, reviewed changes, dependency lockfiles, protected branches, audit retention, and immutable provenance. Releases require dependency/static analysis, secret scanning, archive inspection, and checksums. Exploitable critical/high findings block release unless accountable owners document a time-limited exception. Rotate exposed credentials immediately and preserve evidence.
