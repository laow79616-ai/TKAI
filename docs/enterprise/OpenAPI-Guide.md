# OpenAPI Guide

Generate offline from `server.api.app:create_app` and serialize deterministically. Validate OpenAPI structure, authentication, operation identifiers where present, and expected inventory. Compare output with the approved baseline: any path, method, parameter, body, response, security, or schema delta is an API change requiring separate authorization. Never hand-edit generated schema.
