# User Guide

TKAI exposes framework, API, dashboard, and AI Studio surfaces according to installed configuration. Authenticate through the configured provider and use authorized workspaces and operations only. Protect tokens and exported data. Report issues with version, timestamp, environment, correlation identifier, and sanitized reproduction. Feature details are in `docs/v12`; API consumers should also use the API and OpenAPI guides.
