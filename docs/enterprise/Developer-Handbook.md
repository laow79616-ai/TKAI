# Developer Handbook

Follow the Local Setup Guide. Keep changes narrow, typed, tested, documented, and within public contracts. Never commit `.env`, credentials, databases, dependency directories, or build output. Run `python scripts/validate-enterprise.py` before review. Pull requests include motivation, compatibility and security impact, validation evidence, operational risk, and rollback. Behavior changes require separate product authorization.
