# Deployment Guide

Deploy with the existing TKAI V12 Docker Compose, Helm, or local-runtime process. No new service, port, database, or secret is required. Build the dashboard and Studio assets before packaging. Validate `/openapi.json`, `/business/v1/health`, and `/health/ready` after deployment. Existing V6-V12 configuration remains valid.

