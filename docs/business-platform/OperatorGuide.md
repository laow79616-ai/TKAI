# Operator Guide

Monitor `/business/v1/dashboard` and `/business/v1/health`. A healthy response verifies the catalog, tenant-isolation boundary, and non-execution boundary. Business inventory is metadata; continue to operate V12 backing services with their existing runbooks. No Business Platform endpoint launches a browser, rotates a proxy, executes a task, sends a message, uploads media, or publishes content.

