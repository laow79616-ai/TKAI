# API Guide

The API prefix is `/business/v1`. Routes cover dashboard, module catalog, accounts, browsers, proxies, tasks, content, data, AI Studio, Admin, health, audit, settings, and export metadata. All Business Platform operations use GET. Optional `tenant` and `workspace` query parameters default to `default`. The API is advisory and contains no execution routes.

