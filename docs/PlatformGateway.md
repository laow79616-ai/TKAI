# Cloud Platform Gateway Foundation

Cloud Gateway is an explicit, offline adapter boundary between Cloud descriptors
and Platform. It defines Platform, Cloud, Workspace, Project, Deployment,
Execution, and Storage gateway protocols plus immutable capability, version, and
health descriptors. `ReferencePlatformGateway` returns only supplied local
values; it performs no Runtime, SDK, Studio, Enterprise, network, provider, or
health-probe operation.
