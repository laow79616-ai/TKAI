# Identity

The identity registry represents users, service accounts, agents, applications,
workspaces, and tenants. Records are isolated by tenant and workspace, have an
explicit enabled state, and support non-secret attributes. Platform integrations
should map external directory identifiers to these stable identity IDs.
