# Persistent Configuration Migration

V1.0 users need no API, CLI, or configuration-format changes. Persistent
Configuration is an optional V1.1 capability. Existing settings, credential
sources, ProviderManager, AIClient, and runtime behavior are unchanged.
