# Health Service

Health contracts provide passive `HealthCheck`, `HealthReport`, and
`HealthSnapshot` values. The reference service reports caller-provided checks
and performs no probes, network calls, or process health management.
