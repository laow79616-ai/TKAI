# Release Service

Release contracts describe a package release, manifest, channel, policy, and
snapshot. `ReferenceReleaseService` retains descriptions only; it never
publishes packages, creates tags, or invokes external systems.
