# Version Service

`ReferenceVersionService` provides a local boundary for package version
descriptors. Version comparison, dependency resolution, and package mutation
remain explicit responsibilities outside this Server foundation.
