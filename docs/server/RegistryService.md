# Registry Service

`ReferenceRegistryService` stores caller-provided Registry descriptors in local
memory through the `RegistryStorage` protocol. It has no remote registry,
download, upload, or artifact behavior.
