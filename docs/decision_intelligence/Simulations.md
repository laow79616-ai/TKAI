# Simulations

What-if simulation applies scenario deltas to an explicit baseline and returns
a forecast, field-level comparison, rollback impact, and optimization
suggestions. The reference implementation is deterministic. Production
adapters may delegate forecasting to AI Model Platform or Digital Twin Platform
while retaining the same scoped, audited result contract.
