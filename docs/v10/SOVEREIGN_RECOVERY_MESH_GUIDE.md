# Sovereign Recovery Mesh Guide

The recovery mesh exposes recovery plans, snapshots, rollback references,
readiness, evidence, and validation as metadata. It cannot restore snapshots,
execute recovery, perform rollback, or mutate runtime state.
