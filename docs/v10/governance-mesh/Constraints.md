# Constraints

Constraint metadata covers version, compatibility, security, boundary,
dependency, configuration, runtime, release, and documentation references.
Constraints are descriptive and never applied to a running system.
