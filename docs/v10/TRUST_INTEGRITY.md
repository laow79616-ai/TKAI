# Trust Integrity

Integrity records store a hash algorithm, expected hash, observed-hash
reference, verification status, and evidence references. The mesh neither
reads subject content nor computes, fetches, or remotely verifies hashes.
`unverified` is the safe default.
