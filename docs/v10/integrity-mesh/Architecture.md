# Architecture

The mesh is a bounded, in-process metadata index for completed V6 through V10
components. Immutable contracts enter scope-aware registries and GET-only
projections. It never invokes TikTok, external verification, or runtime mutation.
