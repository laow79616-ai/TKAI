# Operations Guide

Instantiate `SovereignKnowledgeMesh`, explicitly register immutable metadata,
and query a named registry with a bounded limit and scope. Monitor `health()`,
`metrics()`, `diagnostics()`, `traces()`, `structured_logs()`, and `audit()`.

No network, browser, TikTok login, filesystem scan, or production ingestion is
required or permitted.
