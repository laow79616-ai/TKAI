# Sovereign Trust Mesh Guide

The trust mesh projects local trust domains, identities, attestations, scores,
and compatibility metadata. Reads enforce RBAC and scope boundaries. It never
grants trust, attests externally, or contacts a remote verifier.
