# Trust Mesh Windows Guide

From the repository root in PowerShell, activate `.venv` and use the existing
commands in `scripts/` for local operational checks. Do not treat template
PowerShell files outside `scripts/` as operational scripts. The mesh requires no
service account, browser, TikTok session, proxy, credential, or network access.
