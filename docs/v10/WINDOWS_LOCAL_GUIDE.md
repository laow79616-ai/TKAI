# V10 Windows Local Guide

Use PowerShell 7 where available, Python 3.10 or newer, and Node.js 18 or newer.
Create a local virtual environment, install development dependencies, run the
V10 verifier and tests, and build Dashboard and AI Studio from their existing
frontend directories. Store credentials outside the repository. Operational
scripts under `scripts/` must parse successfully before release.
