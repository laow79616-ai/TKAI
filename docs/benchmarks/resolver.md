# Marketplace Resolver Benchmark

Reference-only, offline scenarios cover a linear graph, branch graph,
multi-root request, missing dependency, and cycle detection. Reports are
structural Markdown/JSON benchmark output only; no throughput threshold is
asserted.
