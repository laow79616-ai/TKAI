# Marketplace Verification Benchmark

Reference-only, offline benchmark scenarios: `basic`, `standard`, and `strict`.
They invoke the in-memory verification service through `BenchmarkRunner` with a
fixed seed. The report format is Markdown or stable JSON; it does not set a
machine-specific threshold or claim a production performance target.
