# Windows Guide

Start the local TKAI runtime with `scripts\start-tkai.ps1`, then open the dashboard
URL shown by the local runtime status. Select **tiktok-ai-control-tower** in the
sidebar. Stop the platform with `scripts\stop-tkai.ps1`. No live TikTok connection
is required for validation; automated tests use mocks only.
