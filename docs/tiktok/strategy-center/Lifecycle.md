# Lifecycle

Create a draft, analyze read-only context, review the proposal, record a
decision, and optionally create an approved reference-only handoff. Mark the
reference completed after the owning downstream system reports its outcome,
then archive it. Rejected strategies may return to draft; deleted is terminal.
