# Exports

CSV and JSON are generated directly. XLSX and PDF are artifact references owned
by shared export infrastructure. Every export requires `tiktok:analytics:export`
or admin permission and creates an audit record.
