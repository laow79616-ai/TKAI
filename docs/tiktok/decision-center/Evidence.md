# Evidence

Evidence is captured from bounded read-only adapters. Raw source references are
converted to workspace-bound `sealed-ref://` values before storage or display.
Evidence records contain a type and safe summary, never credentials, cookies,
tokens, browser sessions, or other secrets.
