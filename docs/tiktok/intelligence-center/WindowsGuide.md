# Windows Local Guide

Use the repository's `scripts/setup-tkai.ps1`, `start-tkai.ps1`,
`health-tkai.ps1`, `backup-tkai.ps1`, `restore-tkai.ps1`, and `stop-tkai.ps1`
commands. The Intelligence Center adds no service, port, secret, browser, or
device requirement beyond the existing local TKAI runtime.
