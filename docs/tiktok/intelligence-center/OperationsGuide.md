# Operations Guide

The Intelligence Center runs inside the existing TKAI API process. Monitor its
dashboard, OpenAPI routes, audit records, and Prometheus metrics through
existing observability facilities. It needs no live TikTok connection and
stores no credentials. Suspend upstream collection when restrictions or
challenges are reported; the center does not circumvent them.
