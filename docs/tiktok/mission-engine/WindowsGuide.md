# Windows Guide

Start the local platform with `scripts\start-tkai.ps1` and stop it with
`scripts\stop-tkai.ps1`. Open the dashboard on the loopback URL reported by the
local runtime. The mission engine requires no external TikTok connection for
tests and stores no credentials. Run validation from an activated repository
virtual environment and use the existing dashboard and Studio npm installations.
