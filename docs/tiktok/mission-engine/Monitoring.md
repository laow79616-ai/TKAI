# Monitoring

Mission health reports the mission state and aggregates runtime, execution,
resource, and recovery health. Prometheus-compatible metrics expose total,
running, completed, failed, recovered, and latest latency values using the
required `tiktok_missions_*` and `tiktok_mission_latency_seconds` names.
