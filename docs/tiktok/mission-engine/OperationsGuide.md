# Operations Guide

Use the dashboard to inspect queue ordering, health, dispatch allocations,
recovery state, and analytics. Investigate unhealthy dependencies before retry.
Never retry while Risk Control reports an unresolved restriction or challenge.
Use rollback when a bounded retry is inappropriate. Audit records are scoped by
tenant and workspace and should be retained with the local deployment backup.
