# Dispatch

Dispatch requires an authorized worker selection and queue assignment. The
engine performs approval, risk, window, dependency, and health checks, then
delegates references to existing scheduler, automation, workflow, execution,
runtime, resource, browser, and device services. Autonomous Operation and Risk
Control are policy and health gates, not duplicate execution targets.
