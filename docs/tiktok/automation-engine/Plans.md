# Plans

Reusable plans bind an existing workflow and bound maximum executions,
concurrency, timeout, retry attempts, backoff, cooldown, schedule, approval, and
risk-policy references. Plans validate conservative local limits before use.
