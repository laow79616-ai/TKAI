# Triggers

Manual, scheduled, event, health, recovery, and workflow triggers are supported.
Custom triggers must reference an explicit `automation-trigger://` interface.
Trigger configuration is metadata-validated and cannot contain credentials.
