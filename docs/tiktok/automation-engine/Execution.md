# Execution

Plans select sequential, parallel, or conditional intent. The local coordinator
uses checkpoints, bounded retries, rollback adapters, graceful stop, and timeout
contracts. Queues provide execution, priority, retry, recovery, and delayed
lanes while plan concurrency limits prevent unbounded actions.
