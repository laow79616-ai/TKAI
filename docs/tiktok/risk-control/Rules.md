# Rules

Rules allow only enumerated signal match, threshold match, and trend match operators. Windows, thresholds, trend counts, priorities, cooldowns, versions, scopes, status, and actions are validated and bounded. Arbitrary expressions and executable code are not supported.
