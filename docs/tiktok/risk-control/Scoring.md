# Scoring

Risk score combines weighted severity, confidence, frequency, recency decay, and trend matches. The result is bounded to 0–100 and mapped through configurable ordered Low, Medium, High, and Critical thresholds. Each history record includes explanation, recommended action, level, score, and timestamp.
