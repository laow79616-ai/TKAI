# Utilization

Samples contain normalized CPU, memory, browser-slot, device, worker, queue, proxy,
and account usage. Values outside zero to one are rejected. The latest sample drives
the current utilization ratio while the most recent 100 samples form the trend.
