# Telemetry

Telemetry exposes inventory size, allocation, reservation, and lease counts,
utilization, health, failures, and recovery. Prometheus metrics use the required
`tiktok_resource_*` names and never contain credentials or raw resource references.
