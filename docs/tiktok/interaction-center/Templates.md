# Templates

Reply, comment, and message templates declare variables and localized content.
Tenant-scoped import, export, and clone operations use the same validated domain
model and never contain credentials.
