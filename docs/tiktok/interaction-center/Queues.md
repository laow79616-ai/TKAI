# Queues

Priority selection runs the highest-priority eligible task. Workspace filtering,
delayed eligibility, bounded retries, and service concurrency limits prevent one
workspace or failing task from taking uncontrolled capacity.
