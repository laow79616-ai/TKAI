# Lifecycle

Projects and tasks use `Draft → Review → Approved → Queued/Scheduled → Running →
Completed/Failed → Archived → Deleted`. The service rejects undefined transitions,
versions every project transition, and audits the actor and scope.
