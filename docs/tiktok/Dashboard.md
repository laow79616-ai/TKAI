# Dashboard

The dashboard presents tenant-scoped counts for accounts, statuses, risks, groups, tags, active sessions, expired cookies, browser bindings, and Prometheus statistics. It never exposes login state.
