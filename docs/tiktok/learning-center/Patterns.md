# Patterns

A pattern is created only when an outcome has enough historical samples. Each
pattern records its subject, repeated outcome, sample size, average score,
confidence, plain-language explanation, and immutable evidence references.

Patterns are observations, not instructions. Correlation is not represented as
causation, confidence cannot exceed one, and missing evidence produces no
pattern. Operators should reassess patterns as source histories change.
