# Application Deployment

Deployments bind an application version to an environment and record replicas,
execution quota, status, creator, runs, and failures. Scaling requires at least
one replica. Application metrics share the platform Prometheus endpoint.
