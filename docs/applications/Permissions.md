# Application Permissions

Per-application principal grants support `view`, `edit`, `publish`, `deploy`,
`run`, and `admin`. Owners control sharing by default. Sharing scopes are
Private, Team, Organization, and Public.
