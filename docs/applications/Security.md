# Application Security

Applications default to Private. Secrets must use existing platform facilities,
not metadata. Runtime operations are audited, quotas constrain execution, and
marketplace discovery requires both Published status and Public sharing.
