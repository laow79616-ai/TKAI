# Events

Event subscriptions define topics, filters, schemas, optional ordering keys, and
correlation IDs. Publishers and consumers must use idempotency keys for
deduplication. Schema registries and brokers are adapters to the bounded event
contract.
