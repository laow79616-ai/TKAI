# API Integration

REST and GraphQL connectors declare base URL, authentication reference, static
headers and parameters, request and response mappings, pagination, timeouts,
retry policy, rate limit, allowlist, and request/response size bounds.
Provider calls are supplied through registered handlers and are disabled by
default.
