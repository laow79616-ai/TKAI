# Storage

Storage registrations support S3-compatible, Azure Blob, and GCS adapters.
Objects are represented by references, with bounded upload/download sizes and
retention policies. Credentials are external secret references.
