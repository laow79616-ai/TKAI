# Operations

The dashboard reports integrations, connectors, credential references,
webhooks, events, messaging, databases, storage, health, failures, and metrics.
Prometheus-compatible metrics cover totals, requests, failures, retries,
latency, webhook delivery/failure, and dead letters. Queue depth is supplied by
the configured broker adapter. Deploy using the existing Docker, Kubernetes,
CI/CD, and observability assets.
