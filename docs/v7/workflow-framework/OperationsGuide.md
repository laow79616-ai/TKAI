# Operations Guide

Use `/v7/workflows/health` and `/v7/workflows/metrics` for status and counters.
Inspect registry, definitions, planner, dependencies, constraints, history, and
recovery endpoints for metadata diagnostics. Healthy responses report
`execution_enabled: false`.
