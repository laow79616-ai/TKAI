# Workflow Model

`Workflow` contains ID, name, semantic version, owner, category, definition,
external state reference, tenant/workspace scope, dependencies, constraints,
lifecycle, schedule metadata, metrics, audit references, and metadata. Secret-like
metadata keys are redacted. Contracts are frozen to prevent accidental mutation.
