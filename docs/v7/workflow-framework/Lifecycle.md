# Lifecycle

Supported states are Draft, Validated, Ready, Planned, Queued, Paused, Cancelled,
Archived, and Deleted. Transitions use `LIFECYCLE_TRANSITIONS`. Queued is metadata
only; no runtime queue is mutated.
