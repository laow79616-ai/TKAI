# Consistency

Validation checks transition history, dependency existence, snapshot reference
integrity, snapshot hashes, and state/snapshot version order. `validate_all`
provides cross-module registry validation. Reports do not mutate state and feed
health and consistency metrics.
