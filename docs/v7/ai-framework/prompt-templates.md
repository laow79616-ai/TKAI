# Prompt Templates

Templates are versioned metadata with declared variables, constraints,
compatibility, metadata, and history. Registration validates that declared
variables occur in the template. Rendering and inference are outside scope.
