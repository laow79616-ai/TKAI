# Operations Guide

Register scoped provider metadata before models. Register safety and evaluation
metadata, then an approval record. Inspect health, metrics, audit, dashboard,
and GET-only API projections. Never place credentials in definitions; use
`secret://` references managed by the deployment secret store.
