# Providers

Provider definitions support local, OpenAI-compatible, generic HTTP, mock, and
test kinds. Definitions contain endpoint and `secret://` references only.
Credentials and network clients are never embedded.
