# Versioning

Versions use the V7 `Version` and `VersionRange` semantic version contracts.
The default compatibility rule requires the same major version and a provider
version greater than or equal to the requested version. Capabilities can carry
explicit upgrade paths, replacement IDs, deprecation dates, and retirement
dates.
