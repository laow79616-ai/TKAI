# Logging

Structured records include classification, component, severity, timestamp,
scope, correlation/trace IDs, retention metadata, and safe metadata. The shared
V7 secret filter redacts sensitive values before records become visible.
