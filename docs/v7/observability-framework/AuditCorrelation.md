# Audit Correlation

`AuditCorrelation` links audit, trace, metric, health, and event references
through a shared correlation ID and tenant/workspace scope. References remain
opaque; the framework does not fetch or alter the linked records.
