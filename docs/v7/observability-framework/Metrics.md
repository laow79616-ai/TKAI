# Metrics

`MetricRegistry` supports scoped registration, discovery, samples, and
last/sum/average/min/max aggregation. Definitions include sampling, retention,
V6/V7 compatibility, unit, and reference-only metadata. Collection never
connects to an external metrics service.
