# Eligibility

Eligibility evaluation supports capability, workflow, service, resource, and
configuration subjects. It checks in-scope policy, constraint, runtime
readiness, pause, and kill-switch declarations. The result is an immutable
read-only assessment with reasons. An eligible result is advisory and never
authorizes or initiates execution.
