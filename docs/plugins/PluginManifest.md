# Plugin Manifest

An Enterprise Marketplace definition contains ID, name, version, author,
description, homepage, license, dependencies, permissions, tools, string
metadata, checksum, signature, entry point, category, and tags. IDs are stable;
versions identify immutable releases. Dependencies name an exact plugin ID and
version. Checksums use SHA-256 and signatures cover the original package bytes.
