# TKAI V11 Windows Guide

Use PowerShell 7 from the repository root and the existing `.venv`. Validate with
`.venv\Scripts\python.exe -m pytest tests\v11`. Bind services to loopback and use
the existing signed PowerShell lifecycle scripts. V11 never invokes those scripts.
