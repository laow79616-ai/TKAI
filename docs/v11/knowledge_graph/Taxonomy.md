# Taxonomy

The taxonomy registry supplies stable category labels. Each node carries a
deterministic path rooted at `tkai/v11`. Categories classify ecosystem, platform,
runtime-reference, governance, intelligence-reference, and operations-reference
metadata.

Taxonomy data is descriptive and does not change routing or runtime behavior.
