# Dashboard

The dashboard adapter projects graph overview, nodes, edges, relationships,
taxonomy, ontology, dependencies, validation, diagnostics, health, metrics, and
audit data.

Snapshots declare `read_only = true`, contain no actions, and set mutation support
to false. The adapter has no browser dependency and performs no network requests.
