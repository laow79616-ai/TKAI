# Ontology

The ontology is the closed relationship enum shared by edge validation and
projections. Edges are directional, but automatic inference and traversal are
disabled.

Adding semantics requires a source change, review, and a new immutable snapshot;
clients cannot extend the ontology through the API.
