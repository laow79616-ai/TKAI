# V11 Autonomous Reasoning Fabric Architecture

The fabric is an immutable, local-first metadata coordination layer. It accepts
only caller-supplied references, validates bounded profiles, and emits safe
read-only projections without executing inference, decisions, plans, or actions.
