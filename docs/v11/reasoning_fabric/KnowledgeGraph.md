# Knowledge Graph Integration

Node, edge, relationship, taxonomy, ontology, provenance, lineage, dependency,
and validation identifiers are bounded read-only references. Graph execution
and mutation are unavailable.
