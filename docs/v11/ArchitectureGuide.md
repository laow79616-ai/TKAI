# TKAI V11 Architecture

The immutable component registry is the integration spine. Pure services project
metadata into the dashboard and GET-only API. Intelligence fabrics depend only on
references and evidence; governance fabrics validate them; framework engines
provide context, registry, relationships, dependencies, contracts, interfaces,
diagnostics, metrics, audit, and security. No execution plane exists in V11.
