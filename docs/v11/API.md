# API

V11 exposes nineteen `/v11/intelligence` routes. Every route accepts `GET`
only. There are no POST, PUT, PATCH, DELETE, execution, login, scheduling,
deployment, or control endpoints. The standalone OpenAPI contract and aggregate
server application expose the same route set.
