# Operations Guide

V11 requires no service, database, scheduler, browser, account, or network
configuration. Install TKAI normally and read the V11 projections through the
Python facade or GET-only API. Validate with Ruff, mypy, pytest, OpenAPI checks,
frontend production builds, PowerShell parsing, and `git diff --check`.
