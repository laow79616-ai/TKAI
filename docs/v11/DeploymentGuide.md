# TKAI V11 Deployment Guide

V11 adds no deployment executor. Install the signed package in an existing TKAI
local environment, validate checksums, configure loopback binding, and use the
existing operator-controlled V10 deployment procedure. No service is started or
changed automatically.
