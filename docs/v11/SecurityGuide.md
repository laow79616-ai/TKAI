# TKAI V11 Security Guide

Bind locally, use existing authentication, retain least privilege, and never put
secrets in metadata. V11 filters sensitive metadata and exposes no execution,
mutation, account-operation, browser-automation, or hidden-reasoning endpoint.
Verify release SHA-256 digests before installation.
