# Routing

Prioritized rules support provider, model, capability, tenant, workspace, cost,
latency, and policy routing. Only active models within the caller's isolated
scope and configured allowlists are eligible. Policy routes require a matching
governance policy mapping.
