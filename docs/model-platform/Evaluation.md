# Evaluation

Evaluation records cover quality, optional accuracy, latency, reliability,
optional safety, estimated cost, regression state, and baseline references.
Accuracy and safety are host-supplied interfaces. Comparisons return deltas and
do not make compliance claims.
