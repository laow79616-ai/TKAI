# Benchmarks

Benchmarks retain a dataset reference, scenario, score, latency, token usage,
optional estimated cost, and model version. Version comparison reports score,
latency, token, and cost deltas without copying benchmark datasets.
