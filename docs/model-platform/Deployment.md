# Deployment

Deployments reference hosted models, external APIs, local models, or Kubernetes
workloads. They record scaling configuration and health without embedding runtime
credentials. `KubernetesDeployment` is the integration interface for deploy,
scale, and health operations.
