# Simulation

Simulation evaluates a scenario against the latest snapshot without mutating
live state. Each run contains a prediction, field-level impact, baseline
comparison, rollback plan, and optimization advice. Integrations can replace the
reference projection logic with governed reasoning and model services.
