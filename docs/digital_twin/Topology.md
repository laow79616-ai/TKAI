# Topology

Topology consists of typed entities and directed relationships. Supported entity
types are person, device, machine, system, application, workflow, knowledge,
model, and custom. Relationships support parent, child, dependency, reference,
ownership, topology, and association semantics. Endpoints must belong to the
same twin, tenant, and workspace; self-references are rejected.
