# Federation

Federation accepts references grouped as `v6_governance_centers`,
`v7_frameworks`, `v8_frameworks`, and `v9_components`. A reference contains an
identifier, version, URI, kind, generation, and filtered metadata.

Aggregation replaces only the mesh's local projection. Source systems remain
untouched. Cross-source relationships are metadata edges and have no runtime
semantics.
