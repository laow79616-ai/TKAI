# Policies and Constraints

Policy records contain framework, rule, constraint, and compliance references
plus version metadata. Constraint records link policy and runtime-boundary
references. Both are advisory: policy enforcement is permanently disabled.

Runtime boundaries describe tenant, workspace, capability, framework, module,
extension, and configuration isolation without controlling those runtimes.
