# Federation

Federation normalizes descriptors for V6, V7, V8, and V9. It copies only
reference metadata into a local projection. Source systems retain ownership;
federation performs no network discovery, payload duplication, execution,
approval, or runtime mutation.
