# Windows Guide

From `C:\Users\laow7\Documents\TKAI`, activate `.venv\Scripts\Activate.ps1`.
Use `.venv\Scripts\python.exe` for Python validation and `npm.cmd` for frontend
builds. Operational scripts are PowerShell files under `scripts/`; template
files elsewhere are not part of operational validation.
