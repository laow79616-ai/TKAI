# Planning Federation

Federation accepts a bounded collection of allowlisted references to V6, V7, V8,
and V9 sources. Remote discovery and network-backed references are rejected.
The federation never mutates upstream sources.
