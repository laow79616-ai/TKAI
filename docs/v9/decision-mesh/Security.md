# Security

The mesh supports read/review RBAC and enforces tenant, workspace, and decision
isolation. Secret-bearing metadata keys are recursively rejected. Registration
and federation produce structured audit records.
