# Prompt Studio

Prompt Studio stores immutable revisions with discovered `{{variable}}`
placeholders. Preview requires every declared variable. Testing performs
repeatable case assertions, history is append-only, and unified diff compares
any two one-based versions. Reusable templates can be transported inside a
project bundle without embedding credentials or provider configuration.
