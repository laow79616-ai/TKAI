# Knowledge

A knowledge base contains collections separated by namespaces. Documents are
passed through explicit chunking and embedding interfaces and then written to a
retrieval index. Storage, vector database, embedding provider, and binary file
ownership are adapters, allowing local and cloud deployments to share the same
domain contract.
