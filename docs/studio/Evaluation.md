# Evaluation

Evaluation Studio runs prompt tests, regression suites, benchmarks, and model
comparisons. Results include score, latency, input/output token counts, and an
optional cost supplied by the provider billing adapter. Comparisons are stable
and regression checks support an explicit tolerance.
