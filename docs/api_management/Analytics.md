# Analytics

Analytics record request identifiers, consumers, APIs, routes, latency, status
codes, and error classification. Dashboard data includes quota usage, rate-limit
and quota rejection metrics, and never includes bodies or secret values.
