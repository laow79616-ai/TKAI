# Versioning

Versions require `major.minor.patch` semantic versions. A version may be active
or default, and records compatibility, deprecation date, and migration notes.
Selecting a new default clears the prior default for the same managed API.
