# Subscriptions

Subscriptions link a consumer and application to an API plan, entitlements,
quota, status, and optional expiration. Non-public APIs require a matching
active, unexpired subscription.
