# Operations

Deploy through the existing Docker, Kubernetes Helm, and CI/CD assets. Monitor
the eight `api_*` and `managed_apis_total` Prometheus metrics together with
gateway analytics and audit events. Upstream transports, load balancers,
circuit breakers, OAuth/OIDC providers, and secret stores remain replaceable
interfaces for production operators.
