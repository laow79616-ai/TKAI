# Caching

Cache policies define TTL, key fields, maximum entries, invalidation, and
sensitive-response exclusion. Error responses and responses with authorization
or cookie headers are not cached when sensitive exclusion is enabled.
