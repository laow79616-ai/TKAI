# Authorization

RBAC permissions protect control-plane operations. Tenant and workspace checks
apply to every resource. Invocation scopes can also represent application and
agent identity, while policy-required OAuth-style scopes are enforced at the
gateway.
