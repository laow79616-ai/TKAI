# Security

Every record is tenant- and workspace-scoped. Fine-grained RBAC protects read,
write, reason, plan, model, synthesize, predict, optimize, coordinate, decide,
adapt, improve, align, evaluate, approve, and monitor operations. Policy and
approval checks run before side effects. Audit metadata removes secret, token,
password, credential, and key fields so secrets never enter logs.
