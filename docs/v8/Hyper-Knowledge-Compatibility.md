# Hyper Knowledge Compatibility

Compatibility records relate V6, V7, and V8 references and describe status,
notes, and metadata. Normalization accepts legacy `id` fields as well as current
`identifier` fields. Compatibility metadata is descriptive and does not alter
the referenced framework.
