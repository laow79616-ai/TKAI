# Hyper Knowledge Graph

Entities and directed relationships form a reference-only graph projection.
Edges can cite evidence and semantic metadata, while entities can cite ontology
and framework metadata. There is deliberately no graph query engine, traversal
runtime, action dispatch, or executable edge behavior.
