# Knowledge Fabric

Knowledge records are immutable graph-node descriptors. Evidence, context, and
relationship fields contain references only; evidence payloads and source
systems remain with their owners. Relationship records describe directed
semantic links without traversal side effects.

Reasoning records contain safe summaries, evidence references, confidence,
evaluation references, and explanation references. Chain-of-thought, hidden
reasoning, and internal reasoning metadata are rejected and never stored.
