# Recovery

Recovery records contain status plus rollback, readiness, compatibility, and governance references. Rollback references are descriptive links only.

The fabric never performs rollback, restarts a runtime, or starts a recovery workflow.
