# Operational Readiness

Readiness records cover workflows, capabilities, resources, runtimes, recovery, governance, and compatibility. A `ready` value is an observation supported by evidence references and blockers; it is never an execution authorization.

Consumers must separately apply their existing governance and runtime controls. The fabric provides no execution surface.
