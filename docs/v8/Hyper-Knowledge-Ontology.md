# Hyper Knowledge Ontology

Ontology concepts describe names, categories, domains, allowed relationship
types, constraints, aliases, parent references, and versions. Taxonomy and
hierarchy are metadata. The fabric does not infer, traverse, or execute ontology
rules.
