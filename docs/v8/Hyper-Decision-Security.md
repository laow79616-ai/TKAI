# Hyper Decision Security

Read access is RBAC compatible. Authorization checks tenant, workspace, and decision
namespace independently. Secret-like values are filtered before storage projections,
structured logs, snapshots, or API responses. Registration and aggregation generate
audit records; no live TikTok credentials or access are required.
