# Evolution

An approved evolution cycle can change capabilities, policies, knowledge, workflows, and architecture. Every cycle declares parent and candidate versions. The platform records a version-lineage edge, increments the profile generation, updates its version, emits metrics, and writes a scoped audit entry.
