# Enterprise Store

The Enterprise Store presents typed views for Agent, Plugin, Workflow, Prompt,
Dataset, Knowledge, Model, Template, and Extension packages. All views share
catalog discovery, publisher trust, release lifecycle, licensing, ratings, and
download accounting. Package compatibility is an explicit set of platform
constraints and dependencies form a deterministic acyclic graph.
