# Model Governance

A governed model profile records provider, version, owner, approval status,
allowed and restricted uses, fallback policy, and evaluation reference.
Provider and model allowlist controls should be checked before runtime model
selection and again before fallback.
