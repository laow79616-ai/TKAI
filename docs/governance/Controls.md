# Controls

Supported interfaces cover access control, data boundaries, execution limits,
tool permissions, model and provider allowlists, prompt policy, content policy,
retention policy, and audit policy. Configuration is declarative; adapters are
responsible for enforcing a control at the relevant runtime boundary.
