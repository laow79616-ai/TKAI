# Exceptions

Policy exceptions require a scope, reason, approver, expiration, compensating
control, and status. Exception approval is separate from creation. Consumers
must treat expired exceptions as invalid and continue applying the policy.
