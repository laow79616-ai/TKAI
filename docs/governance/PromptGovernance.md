# Prompt Governance

Prompt governance records prompt version, owner, approval status, risk
classification, sensitive variables, and change history. Sensitive values
must remain references; reports redact recognized secret metadata keys.
