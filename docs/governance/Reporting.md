# Reporting

Reports summarize policy coverage, risk severity, control status, open
findings, exceptions, incidents, and approvals. Audit export returns immutable
evidence references only, is bounded to 1,000 records by default, and applies
sensitive-metadata redaction. Reports never include secrets and do not assert
certification or legal compliance.
