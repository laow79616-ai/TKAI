# Templates

Connector, mapping, and flow definitions are portable versioned templates.
Import validates the same model and security constraints as direct creation;
export omits resolved secrets; clone creates a new identity and version.
