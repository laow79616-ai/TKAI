# Operations

Operators monitor connector health, latency, failures, retry volume, and dead
letters through the dashboard and metrics endpoint. Failed runs carry opaque
run identifiers for correlation. Existing Docker, Kubernetes, CI/CD, logging,
and observability controls remain the deployment baseline.
