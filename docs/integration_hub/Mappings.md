# Mappings

Mappings declare source and target schemas, field mappings, defaults, required
fields, validation, and a fixed transformation vocabulary. Python, shell,
JavaScript, expressions, and other arbitrary executable code are rejected.
