# Credentials

Credentials contain only secret-manager, OAuth2, API-key, certificate, or
service-account references and an optional rotation interface. Plaintext secret
values are never accepted or logged.
