# Integration Flows

A flow joins source and target instances with a trigger, mapping, policy,
retry/dead-letter behavior, optional schedule, lifecycle status, and metadata.
Execution is permitted only for enabled flows.
