# Triggers

Supported types are Schedule, Webhook, API, Event, Workflow, Agent, Knowledge,
Application, and Manual. Trigger configuration is scoped to the automation.
Credentials must be indirect `secret://` or `vault://` references.
