# Policies

`PolicySet` groups retry, timeout, concurrency, rate limit, execution, and
security policies. Defaults are bounded and may be replaced per orchestrator
instance. Coordinator and service limits apply globally and per tenant.
