# Queues

The queue orders available items by schedule time and priority, preserving FIFO
order within a priority. A future availability time implements delayed and
scheduled queues. Exhausted executions are retained in the dead letter queue.
