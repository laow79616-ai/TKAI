# Planning

Planning decomposes goals into an acyclic execution graph with milestones,
dependencies, schedules, and optimization metadata. Unknown task references
and cyclic plans are rejected before execution.
