# Security

Every record is tenant- and workspace-scoped. Fine-grained RBAC protects read,
write, observe, reason, plan, predict, learn, reflect, adapt, execute, approve,
coordinate, and monitor operations. Policy and approval checks run before side
effects. Audit metadata removes secret, token, password, credential, and key
fields so secrets never enter logs.
