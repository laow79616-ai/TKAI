# Publishing

A publisher profile belongs to one tenant, organization, and workspace. A store
application must reference a publisher in the same scope. Production packages
require verified publisher ownership, a signing identity, a valid manifest,
checksum, signature, compatibility metadata, bounded size, and bounded
dependencies. Release history is appended after verification.
