# Security

Controls include tenant, organization, and workspace isolation; scoped RBAC;
publisher verification; signing and checksum validation; compatibility and
permission review; bounded package size and dependency depth; cycle detection;
secret redaction; and rejection of arbitrary shell execution or unrestricted
network declarations.
