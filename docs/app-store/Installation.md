# Installation

Install validates scope, permission review, publisher identity, integrity,
compatibility, dependency existence, cycles, size, and depth before recording an
installation. Enable, disable, uninstall, update, and rollback are explicit state
operations; no arbitrary instructions are executed.
