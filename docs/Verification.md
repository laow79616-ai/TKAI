# Verification Foundation

Reference Only and Offline Only structural verification. It creates immutable,
deterministic reports without downloading, executing code, using a network, or
modifying Registry, Resolver, or Installer state.
