# Shared Context

Shared context contains variables, artifact descriptors, knowledge references,
application state, and workflow state. Updates merge state and retain existing
references so humans, agents, applications, and workflows have a consistent
session view.
