# Workspace

A workspace belongs to a tenant and organization. It contains an ID, name,
description, members, member-to-role mappings, lifecycle status, and extensible
metadata. The workspace ID is the security boundary used by every subordinate
collaboration resource.
