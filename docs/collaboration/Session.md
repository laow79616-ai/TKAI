# Collaboration Session

A session coordinates human participants and agent participants. It owns shared
context, a shared-memory namespace, timeline entries, and lifecycle status.
Presence supports online, offline, busy, idle, typing, and agent-active states.
Messaging supports threads, mentions, replies, and attachment interfaces.
