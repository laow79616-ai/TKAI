# Business Platform Operations

Dashboard, AI Studio, and enterprise administration are read through the local API. Operational checks must not execute TikTok actions, publishing, outreach, workflow runs, browser control, proxy switching, account mutations, or bypasses. Run `scripts/validate-release.ps1`, production frontend builds, OpenAPI validation, and `scripts/smoke-test.ps1` before declaring readiness.
