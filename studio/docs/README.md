# TKAI Studio product layer

This directory reserves Studio-local documentation and deployment material. The
initial architecture is intentionally independent from the V1.x Runtime and
uses only public `tkai.sdk` objects through the backend gateway.
